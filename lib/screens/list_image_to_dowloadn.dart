import 'package:flutter/material.dart';

class ListImagesToDownload extends StatelessWidget {
  const ListImagesToDownload({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('List Images to Download'),
      ),
    );
  }
}
